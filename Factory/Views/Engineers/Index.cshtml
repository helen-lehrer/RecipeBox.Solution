@* @{
  Layout = "_Layout";
}

@using Factory.Models;
@model List<Factory.Models.Doctor>;

<h1>Doctors</h1>

@if (@Model.Count == 0)
{
  <h3>No doctors have been added yet!</h3>
} 

@foreach (Doctor doctor in Model)
{
  <li>@Html.ActionLink($"{doctor.DoctorName}", "Details", new { id = doctor.DoctorId }) </li>
}
<p>@Html.ActionLink("Add new doctor", "Create")</p>
<p>@Html.ActionLink("View All Patients", "Index", "Patients")</p> *@